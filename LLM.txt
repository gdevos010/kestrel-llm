### src/llm_txt_tools/

__init__.py - Declares the package and exposes its version. Keeps the top-level import surface small and stable.

cli.py - Provides the llm-check/llm-fix/llm-sync console entrypoints. Resolves repo root and LLM.txt automatically, then runs verification and symbol refresh steps.
Symbols:
  - Functions: _build_common_parser (L17), llm_check_main (L43), llm_fix_main (L70), llm_sync_main (L110)

logging_utils.py - Configures root logging for CLI utilities with a minimal format. Supports LOG_LEVEL and an explicit --log-level override.
Symbols:
  - Functions: _resolve_level (L13), setup_logging (L32)

repo_utils.py - Finds LLM.txt by searching upward from the current working directory. Resolves repo root and file paths from CLI args or CONDOR_REPO_ROOT.
Symbols:
  - Functions: _pick_llm_file_for_repo_root (L43), find_llm_file_upwards (L23), normalize_path (L18), resolve_repo_root_and_llm_file (L52)

update_llm_symbols.py - Parses LLM.txt entries and refreshes per-file Symbols blocks using Python AST inspection. Writes function, class, and method names in an idempotent format.
Symbols:
  - Functions: _flush_active_entry (L208), _format_symbols_block (L165), _remove_existing_autogen_block (L148), _resolve_header_dir (L202), build_arg_parser (L305), extract_symbols (L89), main (L325), update_llm_file (L289), update_llm_text (L234)
  - Classes: ActiveEntry (L64), ExtractedSymbols (L75)

verify_llm_docs.py - Scans a repo for .py files and checks they are documented in LLM.txt sections. Detects missing entries, broken references, repeated headers, duplicates, and sentence/sorting issues.
Symbols:
  - Functions: _build_excluded_dirs (L421), _find_duplicate_entries (L443), _find_repeated_headers (L427), _find_sentence_issues (L449), _group_entries_by_path (L436), _log_overview (L466), _merge_sections_by_dir (L356), _read_preamble_lines (L345), _report_documented_missing_on_disk (L539), _report_duplicate_entries (L502), _report_missing_in_llm (L522), _report_repeated_headers (L482), _report_sentence_issues (L556), _report_unsorted_sections (L584), _resolve_header_dir_abs (L131), _write_sorted_llm_file (L373), build_arg_parser (L715), collect_section_file_order (L289), count_sentences (L340), iter_python_files (L326), main (L766), parse_llm_file (L227), parse_llm_file_detailed (L138), sort_llm_file (L401), verify (L621)
  - Classes: DocumentedFile (L112), LLMEntry (L88), LLMSection (L77), VerifyOptions (L120)
  - Methods: LLMEntry.summary_lines (L97), LLMEntry.summary_text (L106)

### tests/

test_verify_llm_docs.py - Tests sorting and verification behavior using temporary repos and LLM.txt fixtures. Covers repeated headers, duplicate entries, and strict sentence enforcement.
Symbols:
  - Functions: test_sort_llm_file_already_sorted (L107), test_sort_llm_file_basic (L26), test_sort_llm_file_preserves_multiline_descriptions (L73), test_verify_detects_duplicate_file_entries (L220), test_verify_detects_repeated_headers_and_fix_sorting_merges (L172), test_verify_enforce_exact_two_sentences_ignores_symbols_block (L247), test_verify_with_fix_sorting_integration (L134)

